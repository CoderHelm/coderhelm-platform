{
  "name": "coderhelm-mcp-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild handler.mjs --bundle --platform=node --target=node20 --outfile=dist/handler.mjs --format=esm --external:@aws-sdk/*",
    "package": "npm run build && cd dist && zip -r ../mcp-proxy.zip ."
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
